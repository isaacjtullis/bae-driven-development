import 'babel-polyfill';
// import Bot from './classes/Bot';
// import User from './classes/User';

// (function() {
  // let user = new User('Pablo', 'Ledesma');
  // let bot = new Bot('Alessa', user);
  // let initialGreeting = bot.initialGreeting();
  // console.log(initialGreeting);
  // bot.registerPredeterminedResponse('Ember is dead! React is the future', 'I totally agree!');
  // let botResponse = bot.respondToMessage('I am ballin out today!');
  // console.log(botResponse);
  // botResponse = bot.respondToMessage('I am having a bad day...');
  // console.log(botResponse);
  // botResponse = bot.respondToMessage('I think that Ember is dead! React is the future. What do you think?');
  // console.log(botResponse);
// })();
